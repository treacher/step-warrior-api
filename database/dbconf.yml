development:
  driver: postgres
  open: user=postgres dbname=step-warrior-api sslmode=disable
test:
  driver: postgres
  open: user=postgres dbname=step-warrior-api-test sslmode=disable
production:
    driver: postgres
    open: $DATABASE_URL
